# OpenAI (Required for Orchestrator Logic)
OPENAI_API_KEY=sk-...

# Anthropic (Optional for Deep Reasoning Agents)
ANTHROPIC_API_KEY=sk-ant-...

# Google Gemini (Optional for Large Context/Web Agents)
GOOGLE_API_KEY=AIza...

# ChromaDB Settings (Optional)
# CHROMA_PERSIST_DIRECTORY=.archon/memory

# ── Voice Interface (J.A.R.V.I.S. mode) ──────────────────────────────────────
# Required for `archon voice` — must have billing enabled on your Google project
# GOOGLE_API_KEY is reused above; the vars below are voice-specific overrides.

# Gemini voice persona: Puck | Kore | Aoede | Charon | Fenrir
ARCHON_VOICE=Puck

# How the mic activates: vad (always-on) | ptt (hold Space) | wake (say "Hey Archon")
ARCHON_VOICE_ACTIVATION=vad

# Microphone sample rate in Hz (Gemini expects 16000)
ARCHON_VOICE_SAMPLE_RATE=16000

# Speaker playback sample rate in Hz (Gemini outputs 24000)
ARCHON_VOICE_PLAYBACK_RATE=24000

# ── Audio Preprocessing ───────────────────────────────────────────────────
# High-pass filter cutoff in Hz (removes low-freq hum; default 80)
# ARCHON_HPF_CUTOFF=80

# Noise gate RMS threshold (audio below this is attenuated; default 0.008)
# ARCHON_NOISE_GATE_THRESHOLD=0.008

# AGC target RMS level (normalises volume; default 0.15)
# ARCHON_AGC_TARGET=0.15

# Enable spectral noise reduction (requires `pip install noisereduce`; default 0)
# ARCHON_NOISE_REDUCE=0
